# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# For local development, leave commented (uses SQLite)
# For production (Render/Heroku), this will be auto-set by the platform
# DATABASE_URL=postgresql://user:password@host:port/database

# ===========================================
# CORS CONFIGURATION
# ===========================================
# Comma-separated list of allowed origins for CORS
# For local development:
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# For production (update with your actual frontend URL):
# ALLOWED_ORIGINS=https://yourusername.github.io,https://your-custom-domain.com
ALLOWED_ORIGINS=*

# ===========================================
# JWT AUTHENTICATION
# ===========================================
# Generate a secure random key for production:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-it-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===========================================
# EMAIL CONFIGURATION
# ===========================================
# For Development/Testing: Use Mailtrap
# Get credentials from: https://mailtrap.io/inboxes
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_USERNAME=your_mailtrap_username
# SMTP_PASSWORD=your_mailtrap_password

# For Production: Use Gmail SMTP (or other email service)
# Gmail Setup:
# 1. Enable 2-factor authentication on your Google account
# 2. Generate an App Password: https://myaccount.google.com/apppasswords
# 3. Use the app password below (not your regular Gmail password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=noreply@attendance-system.com

# Alternative: SendGrid (recommended for production)
# Get API key from: https://sendgrid.com/
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# EMAIL_FROM=noreply@attendance-system.com
